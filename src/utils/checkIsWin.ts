// export const checkIsWin = (arr: number[]) => {
//   let winPosition: number[] | null = null;
//   Wins_positins.forEach((position) => {
//     let isWin = position.every((cell) => arr.includes(cell));
//     if (isWin && !winPosition) winPosition = position;
//   });
  
//   return winPosition;
// }